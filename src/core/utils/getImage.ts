// ================================
// Image Utility Functions
// ================================

/**
 * Simple image path utility
 * Currently returns the image path as-is, but can be extended
 * for image processing, CDN integration, or path transformations
 */
export const getImage = (img: string): string => img
